import React from 'react';

const Home = () => {
    return (
        <div>
            <h1>Welcome to Cardify</h1>
            <p>This is the homepage of Cardify application.</p>
            <button>Explore Now</button>
        </div>
    );
};

export default Home;
